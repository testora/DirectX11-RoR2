#pragma once

enum class	SCENE
{
	STATIC,
	LOADING,
	MENU,
#if ACTIVATE_TOOL
	TOOL,
#endif
	TEST,
	MAX
};

enum class CROSSHAIR { MAIN, SCOPE, SUPER_CHARGE, SUPER_READY, SUPER_REBOOT, RELOAD, SPRINT, MAX };
