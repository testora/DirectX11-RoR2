#pragma once

enum class	SCENE
{
	STATIC,
	LOADING,
	LOGO,
	STAGE1,
	MAX
};

enum class	COMPONENT
{
	TRANSFORM,
	MAX
};

enum class	BEHAVIOR
{
	PHYSICS,
	MAX
};

enum class	TRANSFORM
{
	RIGHT,
	UP,
	LOOK,
	POSITION,
	MAX
};
