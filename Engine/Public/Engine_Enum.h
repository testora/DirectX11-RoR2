#pragma once

enum class	KEY_STATE { DOWN, HOLD, UP, NONE, MAX };

enum class	SCENE;

enum class	RENDER_GROUP
{
	PRIORITY,
	NONBLEND,
	BLEND,
	UI,
	MAX
};

enum class	COMPONENT
{
	RENDERER,
	TRANSFORM,
	SHADER,
	TEXTURE,
	MODEL,
	MESH,
	VIBUFFER_RECT,
	VIBUFFER_TERRAIN,
	MAX
};

enum class	BEHAVIOR
{
	PHYSICS,
	MAX
};
